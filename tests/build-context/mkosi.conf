# Minimal bootable Fedora machine used by tests/e2e.sh to exercise `nspawn build`.
[Distribution]
Distribution=fedora
Release=44

[Output]
ImageId=e2e-built

[Content]
Bootable=no
SELinuxRelabel=no
RootPassword=root
Packages=
        systemd
        systemd-networkd
        systemd-resolved
        dbus-broker
        passwd
        util-linux
